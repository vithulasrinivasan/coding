#include<stdio.h>
int main()
{
  int n,i=1,sum=0,count=1;
  printf("enter the number:");
  scanf("%d",&n);
  while(count<=n)
  {
      printf("\n%d",i);
      count++;
      i=i+2;
      sum=sum+i;
  }
  printf("\nthe sum of %d odd natural number is %d",n,sum);
}
