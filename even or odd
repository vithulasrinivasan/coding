#include<stdio.h>
int main()
{
    int number;
    printf("enter the number:");
    scanf("%d",&number);
    if(number%2==0)
    printf("\nIt is an even number");
    else
    printf("\nIt is an odd number");
    return 0;
}
