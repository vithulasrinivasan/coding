#include<stdio.h>
int main()
{
    int a,b,c,d,e;
    printf("enter the value for a,b,c,d and e:");
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    if(a>b && a>c && a>d && a>e)
    printf("\na is the greatest");
    else if(b>c && b>d && b>e)
    printf("\nb is the greatest");
    else if(c>d && c>e)
    printf("\nc is the greatest");
    else if(d>e)
    printf("\nd is the greatest");
    else if(a==b && b==c && c==d && d==e)
    printf("\nall are equal");
    else
    printf("\ne is the greatest");
    return 0;
}
