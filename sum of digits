#include<stdio.h>
int main()
{
    int num,i=0,rem;
    printf("enter the number:");
    scanf("%d",&num);
    while(num!=0)
    {
        rem=num%10;
        i=i+rem;
        num=num/10;
    }
      printf("\nsum of the digits is %d",i);
}
