#include<stdio.h>
int main()
{
    int num,i=0;
    printf("enter the number:");
    scanf("%d",&num);
    while(num!=0)
    {
        num=num/10;
        i++;
    }
     printf("\nNo of digits is %d",i);
}
