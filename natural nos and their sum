#include<stdio.h>
int main()
{
  int n,i,sum=0;
  printf("enter the no of natural numbers to print:");
  scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
  printf("\n%d",i);
  sum=sum+i;
  }
  printf("\nsum of first %d natural number is:%d",n,sum);
}
